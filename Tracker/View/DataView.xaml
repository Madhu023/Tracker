<UserControl x:Class="Tracker.View.DataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Tracker.View"
             xmlns:viewmodel="clr-namespace:Tracker.ViewModel"
             xmlns:common ="clr-namespace:Tracker.Common"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <viewmodel:DataViewViewModel/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <common:DateTimeConverter x:Key="TimeConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="8"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="8"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="8"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="8"/>
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="1">
            <Label Name="lbl_Name"> Path :</Label>
            <TextBox Name="txt_FilePath" Width="240" VerticalContentAlignment="Center" IsReadOnly="True" Margin="8,0"
                     Text="{Binding FileName}" 
                     ToolTip="{Binding FileName}" />
            <Button Name="btn_Browse" Command="{Binding BrowseCommand}" Content=" Load "/>
        </StackPanel>
        <ListView Grid.Row="3" Grid.Column="1" ItemsSource="{Binding ExpenseData}">
            <ListView.View>
                <GridView AllowsColumnReorder="true">
                    <GridViewColumn Header="Date" Width="80"  DisplayMemberBinding="{Binding Path=Time, Converter={StaticResource TimeConverter}}"/>
                    <GridViewColumn Header="Description" Width="140" DisplayMemberBinding="{Binding Path=Description}" />
                    <GridViewColumn Header="Type" Width="60">
                        <!--<GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding}" SelectedValue="{Binding}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>-->
                    </GridViewColumn>
                    <GridViewColumn Header="Amount" Width="60" DisplayMemberBinding="{Binding Path=Amount}" />
                </GridView>
            </ListView.View>
        </ListView>
       
        
    </Grid>
</UserControl>
